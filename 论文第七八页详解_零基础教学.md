# 论文第七、八页精讲：势场构建三步法 + 两种路由算法 + 复杂度分析 + 仿真初步

> **论文**：Interference-Aware Multihop Routing in UAV Networks: A Harmonic-Function-Based Potential Field Approach
> **本讲解对应**：IEEE IoT Journal, Page 19412-19414（论文第7-8页）
> **涵盖内容**：III-B 末尾（势场三步构建法），III-C（两种路由算法），III-D（复杂度），IV 开头（仿真预备 + Fig.6）
> **前置知识**：第五页（调和函数数学基础）
> **预计阅读时间**：45-60 分钟

---

## 📋 这两页在论文中的位置

```
Section III. 协议描述
├── III-A  势场数学基础         ← 第五页，已学过
├── III-B  势场构建（三步法）    ← ★ 第七页上半，本次讲解
│   ├── Step 1: 辅助节点设置
│   ├── Step 2: 固定节点设置
│   └── Step 3: 执行迭代
├── III-C  路由路径建立          ← ★ 第七页下半 + 第八页上半，核心！
│   ├── Scheme α + Algorithm 1
│   └── Scheme β + Algorithm 2
├── III-D  开销与复杂度分析      ← ★ 第八页下半
└──────────────────────────────────────────────────
Section IV. 仿真结果
├── 实验设置 + 基准选择         ← ★ 第八页末尾
└── Fig.6  势场收敛性验证       ← ★ 第八页末尾
```

---

# 第一部分：势场构建三步法（第七页上半）

---

## 🎬 开场：回顾一下我们站在哪里

第五页教了我们一件事：

> **公式 (11)**：$\phi(r)^{(n+1)} = \frac{1}{|N(r)|} \sum_{v \in N(r)} \phi(v)^{(n)}$

翻译成人话：**每个节点的新势值 = 所有邻居势值的平均值。**

但这里有一个关键问题我们还没回答：

> "把谁钉住不动？钉在什么高度？"

势场的形状完全由**固定节点**（"钉子"）决定。选对钉子 → 势场能反映真实干扰。选错钉子 → 势场毫无意义。

**第七页上半部分就是在回答这个问题：怎么选钉子、怎么钉。**

---

## 🔨 Step 1：辅助节点设置（Auxiliary Node Settings）

### 1.1 这个步骤解决什么问题？

回顾一下，论文设计了两个场：

| 场 | 作用 | 需要辅助节点吗？ |
|----|------|:---:|
| **干扰场** | 反映干扰强度分布 | ❌ 不需要 |
| **速度场** | 反映节点移动方向性 | ✅ **需要！** |

**为什么速度场需要辅助节点？**

这是一个非常精妙的问题。让我用生活场景解释：

```
场景：你站在马路边，想拦出租车。

情况 A：你只看"路上有多少空车"           情况 B：你还看"空车往哪个方向开"
  ┌──────────────────────────┐              ┌──────────────────────────┐
  │    ←  ←  ←  ←  ←       │              │    ←  ←  ←  ←  ←       │
  │                          │              │                          │
  │  ● 你                    │              │  ● 你                    │
  │                          │              │                          │
  │        →  →  →  →        │              │        →  →  →  →        │
  └──────────────────────────┘              └──────────────────────────┘
  
  只看数量：左右都有5辆空车              还看方向：
  → 左右一样好，随便选                    → 左边的车在远离你 ❌
                                          → 右边的车在靠近你 ✅
                                          选右边！
```

干扰场像**情况 A**——干扰强度是一个**标量**（只有一个数值），不涉及方向。一个节点在所有方向上的干扰影响大致相同，简单的邻居取平均就够了。

速度场像**情况 B**——链路质量取决于**相对运动方向**。同一个节点，北边的邻居在远离它（链路差），南边的邻居在靠近它（链路好）。如果还把邻居的值全部平均，方向信息就**被洗掉了**。

### 1.2 辅助节点怎么工作？

**核心机制**：给每个节点配四个"分身"，各自负责一个方向。

```
                    北 (Q2: 90°-180°)
                        ↑
            辅助节点 ○  │
                       ╲│
                        ╲
      西 ←────────────── ● ──────────────→ 东
    (Q3)      辅助节点 ○ │  ○ 辅助节点    (Q1)
                        ╱│
                       ╱ │
            辅助节点 ○  │
                        ↓
                    南 (Q4: 270°-360°)

图例：
● = 实物UAV节点（父节点）
○ = 虚拟辅助节点（各负责一个象限）
```

**每个辅助节点的邻居是谁？**

关键规则（原文第436-443行）：

> "辅助节点的邻居 = 父节点在**对应象限内**的邻居。"

```
实例：节点 A 的 Q1 辅助节点（负责东北方向 0°-90°）

    实物节点分布：
    
         B(势值0.9)          C(势值0.3)
            ●  ↗45°            ●  →5°
              ╲                 ╱
               ╲               ╱
                ╲             ╱
                 ● 节点A
                ╱
               ╱
              ╱
             ● D(势值0.9)
           210°
    
    节点 A 的邻居：
    - B 在 45° 方向 → 属于 Q1(0°-90°)
    - C 在 5° 方向  → 属于 Q1(0°-90°)  
    - D 在 210° 方向 → 属于 Q3(180°-270°)
    
    Q1 辅助节点的邻居 = {B, C}  ← 只有Q1方向的邻居！
    Q1 辅助节点的势值 = (0.9 + 0.3) / 2 = 0.6
    
    而 Q3 辅助节点的邻居 = {D}  ← 只有Q3方向的邻居
    Q3 辅助节点的势值 = 0.9
```

**关键区别**：

```
没有辅助节点（干扰场的做法）：
    节点 A 的势值 = (0.9 + 0.3 + 0.9) / 3 = 0.7
    → 一个数，失去方向信息

有辅助节点（速度场的做法）：
    Q1 势值 = 0.6  → 东北方向链路较好，优先
    Q3 势值 = 0.9  → 西南方向链路较差，规避
    → 四个数，保留了方向信息！
```

> **一句话总结 Step 1**：给每个节点配四个辅助节点，每个只"看"一个象限的邻居。这样同一个实物节点在不同方向上就有了**不同的势值**——方向性信息被保留下来，路由时可以根据"往哪走"选择最优象限。

**为什么是四个而不是八个或任意角度？** 四象限是最简方案——刚好把360°分成可管理的四个块，计算开销可控（节点数 ×4），又能捕捉方向性差异。

---

## 📌 Step 2：固定节点设置（Fixed Nodes Settings）

这是**整个势场构建中最关键的一步**——选对钉子，场才有意义。

### 2.1 "固定"到底是什么意思？

论文特别强调了一个容易误解的点（原文第447-449行）：

> ⚠️ **"固定"不意味着节点是静止不动的。它指的是在迭代计算中，这些节点的势值保持不变。**

```
"固定" ≠ "节点静止"
"固定" = "势值在迭代中不变"

类比：
┌─────────────────────────────────────────────┐
│ 一个教室里有 30 个学生，                     │
│ 老师要算每个人的"平均身高影响值"             │
│                                             │
│ 固定节点 = 老师指定"张三的值就是 1.8m，      │
│            不管怎么迭代都不要改"             │
│                                             │
│ 张三可以走来走去（节点在移动），              │
│ 但他的"固定值"不变                          │
└─────────────────────────────────────────────┘
```

### 2.2 三类固定节点

论文定义了三类节点作为势场的"锚点"：

```
三类固定节点
├── 类型 1：边界节点 (Boundary Nodes)
│   └── 位于网络最外围的节点 → 钉住场的"边框"
│
├── 类型 2：受轻微影响的节点 (Minimally Affected)
│   └── 通信质量几乎不受干扰/移动影响 → 低ETX → 低势值
│
└── 类型 3：受严重影响的节点 (Significantly Affected)
    └── 通信质量严重受干扰/移动影响 → 高ETX → 高势值
```

**为什么要这样设计？** 用"布匹类比"来解释：

```
布匹类比：
┌──────────────────────────────────────────────────────┐
│                                                      │
│   类型1（边界）= 布匹四边钉在木板上                    │
│   → 提供场的"框架"，没有边界 -> 场无限延伸           │
│                                                      │
│   类型3（严重受影响）= 布匹某处被钉子往上顶            │
│   → 形成"山峰" = 强干扰区域 → 路由应绕行             │
│                                                      │
│   类型2（轻微受影响）= 布匹某处被钉子往下拉            │
│   → 形成"山谷" = 通信良好区域 → 路由应优先           │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 2.3 怎么判断一个节点属于哪一类？

**关键判据**（原文第457-460行）：

> "如果一个节点在被影响前势值为 1，被影响后势值为 5，比值 5 超过预设阈值，则该节点被归类为固定节点（类型 3）。"

```
判断流程：

  节点当前的 ETX 值  ──→  与历史基线比较
                              │
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
          ETX ≈ 基线     ETX ≪ 基线    ETX ≫ 基线
           (变化<阈值)    (下降>阈值)   (上升>阈值)
                │             │             │
                ▼             ▼             ▼
           非固定节点    类型2固定节点   类型3固定节点
          (参与迭代)    (低势值锚点)   (高势值锚点)
```

**具体数字例子**：

```
UAV-A 的历史平均 ETX = 2.0（正常情况下的期望传输次数）
阈值 = 2.0 （即 ETX 变为原来的 2 倍或 1/2 以上就触发固定）

情况 1：干扰源出现 → ETX 飙升到 8.0
         8.0 / 2.0 = 4.0 > 2.0 → 严重受影响 → 类型3固定节点
         势值固定为 8.0（高 → "山峰"）

情况 2：移动到良好区域 → ETX 降到 1.1
         1.1 / 2.0 = 0.55 < 0.5 → 轻微受影响 → 类型2固定节点
         势值固定为 1.1（低 → "山谷"）

情况 3：ETX = 2.3
         2.3 / 2.0 = 1.15 ∈ (0.5, 2.0) → 正常范围 → 不固定
         势值通过迭代由邻居决定
```

> **一句话总结 Step 2**：找出"极端节点"（特别好或特别差的），把它们钉住不动，作为势场的"山峰"和"山谷"。其余节点通过调和迭代自然过渡。

---

## 🔄 Step 3：执行迭代（Execute Iterations）

这一步反而是最简单的——因为我们在第五页已经详细学过了。

```
Step 3 流程：
1. 固定节点的势值已经锁定（Step 2 的结果）
2. 对每个非固定节点，执行公式 (11)：
   新势值 = 所有邻居势值的平均值
3. 重复直到全场势值不再显著变化（约 50 轮迭代）
4. 完成！势场建成！
```

> **一句话总结 Step 3**：固定节点打好钉子，其余节点反复取邻居平均，约50轮后收敛——势场成型。

---

# 第二部分：两种路由算法（第七页下半 + 第八页上半）

这是**全论文最具创造性的工程贡献**。

---

## 🎯 前置理解：两种设计哲学

论文开门见山说出了两种路由方案的本质区别（原文第473-477行）：

```
Scheme α (Alpha)                    Scheme β (Beta)
━━━━━━━━━━━━━━━━━━━━              ━━━━━━━━━━━━━━━━━━
目标：时效性优先                   目标：无损性优先
     (Timeliness)                       (Losslessness)
追求：最短传输距离                 追求：最佳势场匹配
     "尽量快到达"                      "尽量可靠到达，哪怕绕路"

比喻：
  ┌─────────────────┐              ┌─────────────────┐
  │ 橡皮筋上的小球   │              │ 探照灯越野车     │
  │                 │              │                 │
  │ 目标拽着它走    │              │ 自己探测障碍    │
  │ 引力 > 障碍时就 │              │ 看到就绕        │
  │ 直接穿过去      │              │ 不管绕多远      │
  └─────────────────┘              └─────────────────┘
```

---

## 🧲 Scheme α：橡皮筋上的小球（Algorithm 1）

### 2.1 伪代码逐行解读

```
Algorithm 1: Scheme α
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输入: 当前节点, 源节点, 汇聚节点(sink), 邻居信息
输出: 路由节点列表

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 3 行:  WHILE 邻居集合中不包含汇聚节点
         → "还没到目的地，继续找路"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 4 行:  计算当前节点到汇聚节点的方位角 θ_sink
         → "目标在我的哪个方向？"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 5 行:  计算当前节点到每个邻居的方位角 θ_neighbor
         → "每个邻居在我的哪个方向？"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 7 行:  Δθ = θ_neighbor - θ_sink
         → "这个邻居偏离目标方向多少度？"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 8 行:  Rank = [λ₁ + λ₂/(1 + λ₃·e^(-Δθ))] × PV
         → "综合考虑方向好坏和链路好坏，打分！"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第10 行:  下一跳 = 邻居中 Rank 最小的那个
         → "选分数最低的（最好的）"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第11 行:  当前节点 = 下一跳（继续往前走）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 2.2 核心公式 (16)：Sigmoid-like 方位变换 —— 深入拆解

$$\boxed{f(\Delta\theta) = \lambda_1 + \frac{\lambda_2}{1 + \lambda_3 \cdot e^{-\Delta\theta}}}$$

**先看这个函数长什么样**：

```
f(Δθ)
  ^
  │                                ╱
λ₁+λ₂ ┤                         ╱╱
  │                          ╱╱
  │                       ╱╱
  │                    ╱╱
  │                 ╱╱     ← 中间段：对角度偏差敏感
  │              ╱╱
  │           ╱╱
  │        ╱╱
  │     ╱╱
λ₁ ┤──────────────────────────────→ Δθ
  -∞  背向目标        0        朝向目标  +∞
  ← 最大惩罚 →       中性      ← 最大奖励 →
```

**为什么长这样？三个参数的物理含义**：

为了真正理解，我们先做几个简化，再逐步加回参数。

**Level 0：最基本的 sigmoid —— $1/(1 + e^{-x})$**

```
        1.0 ┤                    ╱╱╱╱╱╱╱╱╱╱
            │                 ╱╱
            │              ╱╱
        0.5 ┤           ╱╱
            │        ╱╱
            │     ╱╱
        0.0 ┤╱╱╱╱╱╱─────────────────────→ x
           -∞     0    +∞

特性：
- 输出始终在 (0, 1) 之间
- x=0 时输出 0.5（"不偏不倚"）
- x→+∞ 时输出→1（"非常接近目标方向"）
- x→-∞ 时输出→0（"背向目标"）
```

**Level 1：加上 $\lambda_2$ —— 控制幅度**

$$f(\Delta\theta) = \frac{\lambda_2}{1 + e^{-\Delta\theta}}$$

```
λ₂ 的含义：
- λ₂=1  → 幅度小，角度因素不重要，主要看 PV
- λ₂=10 → 幅度大，角度因素很重要，优先往目标方向走

调参直觉：
  强干扰环境 → λ₂ 小一点，让路由多考虑绕过干扰
  弱干扰环境 → λ₂ 大一点，让路由更直接地走向目标
```

**Level 2：加上 $\lambda_3$ —— 控制对角度偏差的敏感度**

$$f(\Delta\theta) = \frac{\lambda_2}{1 + \lambda_3 \cdot e^{-\Delta\theta}}$$

```
λ₃ 的作用——改变过渡区的陡峭程度：

  λ₃=0.1（缓）：            λ₃=10（陡）：
  1.0 ┤    ╱╱╱╱╱           1.0 ┤          ╱╱╱
      │  ╱╱                     │        ╱╱
      │╱╱                       │      ╱╱
  0.0 ┤╱──────────→          0.0 ┤╱╱╱╱╱──────→

λ₃ 小 → 模糊判断："差不多是目标方向就行"
λ₃ 大 → 精确判断："必须非常正对目标方向才算好"
```

**Level 3：加上 $\lambda_1$ —— 基线偏置**

$$f(\Delta\theta) = \lambda_1 + \frac{\lambda_2}{1 + \lambda_3 \cdot e^{-\Delta\theta}}$$

```
λ₁ 的含义：即使方向完全背向目标，Rank 也不至于为 0

为什么需要 λ₁？
  如果 λ₁=0，那么"完全背向目标"时 f≈0，Rank≈0
  → 这意味着即使背向的邻居 PV 很高（链路很差），
    Rank仍然是0，会被误选！
  
  有 λ₁ 后，最坏情况也有一个基础排名值，
  确保 Rank = 方向分 × PV 的组合逻辑始终成立
```

### 2.3 Rank 公式的完整含义

$$\boxed{\text{Rank} = \underbrace{\left(\lambda_1 + \frac{\lambda_2}{1 + \lambda_3 e^{-\Delta\theta}}\right)}_{\text{方向奖励因子 } f(\Delta\theta)} \times \underbrace{\text{PV}}_{\text{链路质量惩罚}}}$$

**Rank 是两个因素的乘积**：

| | Δθ 好（朝向目标）| Δθ 差（背向目标）|
|---|---|---|
| **PV 小**（链路好）| f 小 × PV 极小 = **Rank 极小** ✅ 最优 | f 大 × PV 极小 = Rank 中等 |
| **PV 大**（链路差）| f 小 × PV 大 = Rank 中等 | f 大 × PV 大 = **Rank 极大** ❌ 最差 |

**关键洞察**：
- 朝向目标 + 低干扰 → Rank 最小 → **选！**
- 朝向目标 + 高干扰 → Rank 可能不如"背向目标 + 低干扰"
- 这就是"绕行"的机制：绕行方向（偏离目标）被 f 惩罚，但如果绕行方向的 PV 足够低，乘积仍可能更小

### 2.4 一个完整的数字算例

```
场景设定：
  当前节点在原点(0,0)，目标节点在正北方向(θ_sink = 90°)
  λ₁ = 0.3, λ₂ = 1.0, λ₃ = 1.0

邻居节点：
┌──────────┬────────┬───────────┬──────┬──────────┬──────────┐
│ 邻居     │ 方位角 │   Δθ      │ f(Δθ)│    PV    │   Rank   │
├──────────┼────────┼───────────┼──────┼──────────┼──────────┤
│ A (正北) │  90°   │    0°     │ 0.80 │   8.0    │   6.40   │
│ B (东北) │  45°   │   -45°    │ 1.12 │   2.5    │   2.80   │
│ C (西北) │ 135°   │   +45°    │ 0.47 │   2.5    │   1.18   │ ★ 当选
│ D (正南) │ 270°   │  +180°    │ 1.30 │   1.8    │   2.34   │
└──────────┴────────┴───────────┴──────┴──────────┴──────────┘

分析：
  A：正对目标，但 PV=8.0（严重干扰！）→ Rank=6.40 太大 ❌
  C：偏西45°，但 PV=2.5（链路好）→ Rank=1.18 最小 ✅
     → 选择向西绕行，而不是向北穿过去！
     
  D：完全背向目标（向南），PV虽低但f大 → Rank=2.34
     → 不如C好，所以不会倒退走

结论：虽然有干扰在正北，但C在西北方向提供了一条"绕行"
      的路径。Scheme α 成功避开了干扰！
```

### 2.5 橡皮筋隐喻 —— 为什么有时会穿越干扰

论文用了一个精妙的物理类比（原文第487-491行）：

> "想象一条线，一端钉在目标节点位置，另一端系着一个小球。线不断缩短，球最终会被拉到目标位置。线和干扰场的综合效应使得球绕开山峰。当方位角被变换后，朝向目标的角度得到一个更小的值（方向奖励），使得朝向目标方向的 Rank 值更小。"

```
橡皮筋类比：
                             目标 ●
      干扰山                      ↑
      ╱╲                          │ ← 橡皮筋拉力
     ╱  ╲                         │
    ╱    ╲                        │
   ╱  ●───╲───→ 小球             │
  ╱   球        被拉向目标        │
                                   

正常情况（山不高）：
  拉力 > 翻山的难度 → 球绕山腰滑过 ✅ → 轻微绕行

极端情况（山太高 OR 球就在山脚下）：
  拉力 ≫ 绕山的难度 → 橡皮筋最短路径就是穿过去
  球直接翻山 ❌ → 穿越干扰区
```

**这解释了 Fig.9(b) 中 Scheme α 为何直接穿越干扰区**：起点正好在干扰区，橡皮筋的拉力太强，"绕行"的收益无法说服算法偏离最短路径。

---

## 🔦 Scheme β：探照灯越野车（Algorithm 2）

### 3.1 伪代码逐行解读

```
Algorithm 2: Scheme β
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输入: 当前节点, 源节点, 汇聚节点, 邻居信息
输出: 路由节点列表

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第一阶段：每个节点计算自己的"阻挡区域"（第 3-5 行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 3 行:  WHILE 网络运行
         → "持续维护阻挡区域信息"
第 4 行:  每个节点基于邻居的势值，计算阻挡区域 b
         → "我看哪个方向的链路质量太差，标记为'此路不通'"
         例如: b ∈ [64°, 84°] ∪ [90°, 196°]
         含义: 角度 64°-84° 和 90°-196° 不可通行
第 5 行:  END WHILE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第二阶段：选路（第 6-15 行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 6 行:  WHILE 邻居集合中不包含汇聚节点
         → "还没到目的地，继续"

第 7 行:  θ_sink = 当前节点到汇聚节点的方位角
         → "目标在我的哪个方向？"

第 8 行:  θ_neighbor = 当前节点到每个邻居的方位角
         → "每个邻居在我的哪个方向？"

第 9 行:  IF θ_sink 在阻挡区域 b 里面：
             → "目标方向被挡住了！不能直走"
第10行:      下一跳 = 阻挡区域边缘最靠近目标的邻居
             → "贴着障碍物边缘走，绕过去"

第11行:  ELSE：
             → "目标方向通畅，可以直接走"
第12行:      下一跳 = 邻居中 |θ_neighbor - θ_sink| 最小的那个
             → "选最靠近目标方向的邻居"（类似贪心地理路由，
               但在无阻挡时才执行）

第13行:  END IF

第14行:  当前节点 = 下一跳（继续往前走）
第15行: END WHILE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.2 核心概念：阻挡区域 (Blocked Area)

**阻挡区域是 Scheme β 最核心的概念。**

```
什么是"阻挡区域"？

从当前节点向外看，如果某个方向上的邻居势值普遍很高
→ 那个方向被干扰"挡住"了 → 标记为阻挡区域

      阻挡区域 [30°, 80°]
         ╱        ╲
        ╱  ⚡⚡⚡⚡⚡ ╲
       ╱  ⚡干扰区⚡  ╲
      ╱  ⚡⚡⚡⚡⚡   ╲
     ╱                ╲
    ● 当前节点           ● 目标
    (不往30°-80°走)     (在0°方向，畅通)
```

**阻挡区域的判定**：

```
如何计算阻挡区域 b？

对当前节点：
1. 扫描所有方向的邻居
2. 如果某个方向的邻居 PV > PV_threshold（势值阈值）
   → 该角度加入阻挡区域
3. 合并相邻的阻挡角度 → 得到阻挡区间

例如：
  角度 60°-85° 的邻居 PV 都 > 5.0
  角度 90°-195° 的邻居 PV 也都 > 5.0
  → b = [60°, 85°] ∪ [90°, 195°]
```

### 3.3 探照灯隐喻

论文用了一个非常形象的类比（原文第501-506行）：

> "想象一辆配备探照灯的车，目标是在调和函数构建的平面上到达目的地。当遇到不可逾越的山峰时，车辆利用探照灯的光线寻找绕行路线，而不是像 Scheme α 那样被拖过山峰。因此，Scheme β 确保车辆在尽可能平坦的表面上行驶。"

```
Scheme α（橡皮筋）：              Scheme β（探照灯）：
                                
     目标 ●                           目标 ●
      ↑                                ↑
      │ ← 拉力                         │
    ╱╲                                ╱╲
   ╱  ╲    ┌─ 球从这里               ╱  ╲    ┌─ 探测到阻挡
  ╱ ⚡ ╲   │  翻过去                ╱ ⚡ ╲   │  主动绕行
 ╱  干扰 ╲ │  (因为拉力 >          ╱  干扰 ╲ │  
╱         ╲┘   翻山成本)         ╱         ╲┘  ← 沿边缘绕行
● 球                               ● 车
```

**两种方案的"障碍物处理策略"对比**：

| | Scheme α（橡皮筋）| Scheme β（探照灯）|
|---|---|---|
| 障碍物感知 | 间接（通过 Rank 计算隐式感知）| 直接（主动计算阻挡区域）|
| 绕行决策 | 隐式（好方向+低PV < 坏方向+好PV）| 显式（"这个方向被挡住了"）|
| 穿越干扰 | **可能**（拉力 > 绕行成本时）| **几乎不可能**（阻挡区明确禁止）|
| 路径特征 | 尽量短，但可能穿越 | 尽量平坦，宁可绕远 |
| 回溯 | 极少（仅在所有方向都不如目标方向时）| 经常（挡住就沿边界反方向绕）|

### 3.4 一个具体的数字算例

```
场景：当前节点在原点，目标在正北(θ_sink = 90°)
      势值阈值 = 4.0

邻居信息：
┌────────┬────────┬──────────┬──────────┬──────────────┐
│ 邻居   │ 方位角 │   PV     │ 是否阻挡 │   处理方式    │
├────────┼────────┼──────────┼──────────┼──────────────┤
│ A      │  85°   │  7.2     │ ✅ 阻挡  │   不可选择    │
│ B      │  90°   │  8.5     │ ✅ 阻挡  │   不可选择    │
│ C      │  95°   │  6.1     │ ✅ 阻挡  │   不可选择    │
│ D      │  70°   │  2.1     │ ❌ 通畅  │  |70-90|=20°  │
│ E      │ 105°   │  2.8     │ ❌ 通畅  │ |105-90|=15°  │
│ F      │  30°   │  3.5     │ ❌ 通畅  │ |30-90|=60°   │
└────────┴────────┴──────────┴──────────┴──────────────┘

阻挡区域 b = [85°, 95°]（A,B,C 三个方向被阻挡）

θ_sink = 90° ∈ [85°, 95°] → 目标方向被挡住！

按 Algorithm 2 第 10 行：
  阻挡区域的边界是 85° 和 95°
  在边界两侧的邻居中选最靠近目标的：
    D(70°) 和 E(105°) 
    → |70° - 90°| = 20°  vs  |105° - 90°| = 15°
    → E(105°) 更靠近目标方向
  下一跳 = E ✅

注意：
  如果没有阻挡机制，只用贪心最近方向，
  会选择 C(95°)，但 C 的 PV=6.1，链路极差！
  Scheme β 因为阻挡机制避免了这个问题。
```

### 3.5 Scheme β 为什么需要两个独立的 While 循环？

注意到 Algorithm 2 有两个 `WHILE`（第 3 行和第 6 行），这不是多余的：

```
第一个 WHILE（第 3 行）：
  "WHILE 网络运行"
  → 持续计算阻挡区域
  → 这是一个后台进程，不等待路由请求
  → 阻挡区域需要实时维护，因为势场随着干扰变化和节点移动在更新

第二个 WHILE（第 6 行）：
  "WHILE 邻居集合中不包含汇聚节点"
  → 这是路由进程，当有数据包需要转发时才执行
  → 它使用第一个 WHILE 维护好的阻挡区域信息
```

**设计智慧**：将"环境感知"（阻挡区域计算）和"路径决策"（选下一跳）分离。阻挡区域作为势场的衍生产品，可以独立维护和更新，不需要在每次路由决策时重新计算。

---

### 3.6 两算法核心差异：一张图看清

```
              路径 1                          路径 2
              
目标 ●                               目标 ●
  ↑                                     ↑
  │                                     │
  │   ╱╲                                │        ╱╲
  │  ╱  ╲    Scheme α:                 │       ╱  ╲
  │ ╱ 干扰 ╲  "拉力够大                  │      ╱ 干扰 ╲
  │╱        ╲  翻过去!"  ------------>   │     ╱        ╲
  ● 球       (穿越干扰)                  │    ╱          ╲
  (在干扰区附近)                         │   ╱            ╲──
                                        │  ╱               ╲
                                        │ ╱    Scheme β:    ╲
                                        │╱    "探测到阻挡，   ╲
                                        ● 车  绕过去!"       ╲
                                                           ╲──

α 的决策逻辑：                      β 的决策逻辑：
  方向奖励 + 势值乘积的综合判断      先判断阻挡，再选方向
  → 可能穿越，可能绕行              → 几乎总绕行
  → "哪个邻居综合得分最高？"        → "哪个方向是畅通的？"
```

---

# 第三部分：复杂度与开销分析（第八页下半，Section III-D）

---

## 💰 开销分析（Overhead）

### 4.1 开销来自哪里？

论文从两个方面分析开销：

```
开销来源
├── 1. 势场建立开销
│   └── 主要取决于：ETX信息的获取
│       ├── ETX来自MAC层的链路探测消息 (Link-probe Messages)
│       ├── 探测间隔 = 0.5秒 [来自参考文献43]
│       └── 这个间隔在"开销"和"反应速度"之间取得平衡
│           - 间隔太短 → 消息泛滥，浪费带宽
│           - 间隔太长 → 反应迟钝，跟不上拓扑变化
│
└── 2. 路由开销
    └── 主要取决于：位置和PV信息的传播方式
        ├── 论文指出：本质上与 GPSR 相当
        └── 因为两者都需要位置信息（GPSR）或位置+PV（本文）
           信息可以通过控制消息附带传播
```

### 4.2 "与GPSR相当"是什么意思？

GPSR（Greedy Perimeter Stateless Routing）是经典的地理路由协议，每个节点需要在控制消息中广播自己的位置。本文的方法需要多广播一个 PV 值——一个浮点数，开销极小。

```
GPSR 的 Hello 消息：    [节点ID, 位置X, 位置Y]
本文的 Hello 消息：     [节点ID, 位置X, 位置Y, PV, 辅助节点PV×4]
                         └─────── 仅多了几个浮点数 ──────┘
```

### 4.3 为什么 0.5 秒是合适的间隔？

这是一个工程性的选择（原文第549-553行）：

> "在文献 [43] 中，这个间隔被设为 0.5 秒，实验证明这个间隔有效平衡了开销量和反应速度，并能有效捕获 UAV 网络的拓扑变化。"

```
间隔选择权衡：

0.1 秒 → 每秒 10 条探测消息
        → 开销太大，网络拥塞
        → 但反应极快

0.5 秒 → 每秒 2 条探测消息  
        → ✅ 开销适中
        → ✅ 反应速度可接受（0.5秒内可检测到邻居变化）

5.0 秒 → 每5秒1条探测消息
        → 开销极小
        → 但UAV可能在5秒内飞出通信范围！反应太慢
```

---

## ⏱️ 复杂度分析（Complexity）

### 5.1 势场构建的复杂度

公式 (11) 的复杂度，论文拆成三个因素分析：

```
φ(r)^(n+1) = (1/|N(r)|) × Σ φ(v)^(n)

复杂度因素：
┌────────────────────────────────────────────────────────────┐
│ 因素 1：迭代次数 n                                          │
│   → 每多迭代一轮，复杂度线性增加 O(n)                        │
│   → 从 Fig.6 可知 n≈50，这是一个常数因子                     │
├────────────────────────────────────────────────────────────┤
│ 因素 2：每次迭代的复杂度                                     │
│   → 每个节点r的复杂度 ∝ |N(r)|（邻居数量）                    │
│   → 邻居越多 → 每次求和的项越多 → 单节点计算时间越长         │
├────────────────────────────────────────────────────────────┤
│ 因素 3：图的结构                                             │
│   → 总节点数 N，平均邻居数 D                                 │
│   → 稠密图：D ≈ N（每个节点几乎连接所有节点）                 │
│   → 稀疏图：D ≪ N（每个节点只有少数邻居）                    │
└────────────────────────────────────────────────────────────┘
```

**总复杂度**：$\boxed{O(n \cdot N \cdot D)}$

| 场景 | N | D | n | 总操作数 | 实际耗时估算 |
|------|---|---|---|------|------------|
| 稀疏网络 | 100 | 5 | 50 | 25,000 | 毫秒级 |
| 中等网络 | 400 | 10 | 50 | 200,000 | 秒级 |
| 稠密网络 | 400 | 50 | 50 | 1,000,000 | 数秒 |

### 5.2 路由算法本身的复杂度

```
Algorithm 1/2 的复杂度分析：

每个 WHILE 循环：
  → 遍历所有 D 个邻居 → O(D)

每个完整的源→目标路由：
  → 需要 H 跳 → H 个循环
  → 总复杂度 O(H × D)

典型值：
  H ≈ 5-10 跳（中等网络规模）
  D ≈ 5-15 个邻居
  → 总操作量 ≈ 25-150 次比较
  → 实际耗时：微秒级（几乎可忽略）
```

**重要对比**：

| | 势场构建 | 路由执行 |
|---|---|---|
| 复杂度 | $O(n \cdot N \cdot D)$ | $O(H \cdot D)$ |
| 执行频率 | 场更新时（周期性 / 事件驱动）| 每个数据包 |
| 计算量级 | 百万级操作 | 百级操作 |
| 耗时 | 秒级 | 微秒级 |

> **关键认识**：重的计算放在势场构建阶段（偶尔做一次），轻的计算放在路由决策阶段（每包都做）。这是典型的**预处理换取在线效率**的设计策略。

---

# 第四部分：仿真预备（第八页末尾 + Fig.6）

---

## 📊 仿真设置概览

### 6.1 为什么选 GPSR 和 AC 作为对比？

| 基准算法 | 为什么选它 | 来源 |
|---------|-----------|------|
| **GPSR** (Greedy Perimeter Stateless Routing) | 同样需要节点位置信息，与本文方案的需求一致；已在 UAV 网络中被广泛优化 | [44], [46], GitHub开源 |
| **AC** (Ant Colony 蚁群算法) | 经典启发式方法，在 NP-hard 的路由问题上表现优秀；本文将其基于势场进行了优化 | [45], GitHub开源 |

**选择逻辑的深层含义**：GPSR 代表"传统地理路由"的基线（不建模干扰），AC 代表"智能启发式"的基线（有寻优能力但复杂度高）。本文方案需要证明：比 GPSR 强（因为有干扰建模），且至少不弱于 AC（但更简单）。

### 6.2 仿真分两大部分

```
仿真计划
├── 第一部分：势场构建可行性验证
│   ├── Fig.6(a): 网络密度 vs 收敛速度
│   ├── Fig.6(b): 初始值 vs 收敛速度
│   └── Fig.6(c): 通信半径 vs 收敛速度
│   目标：证明"调和函数 + Jacobi 迭代"方案可行
│
└── 第二部分：路由算法性能验证
    ├── Fig.7-9: 定性验证（场形态 + 路径可视化）
    └── Fig.13-16: 定量对比（PDR、能耗、延迟、距离）
```

---

## 🔬 Fig.6：势场收敛性——三组实验，三个问题

这是验证**第五页的公式(11)到底能不能用**的三组实验。

### 6.2 实验的通用设置

```
所有三个实验的共享设置：
  - 10个随机节点被指定为固定节点
  - 固定节点势值 = 1
  - 其余所有节点初始势值 = 0
  - 运行 Jacobi 迭代
  - 观测指标：E(PV) = 全场势值的归一化期望值
    （当 E(PV) 不再变化时 → 收敛）
```

---

### Fig.6(a)：网络密度 vs 收敛速度

**实验问题**：节点多还是少，哪个收敛更快？

```
X轴 = 迭代次数
Y轴 = 归一化平均势值 E(PV)
不同曲线 = 不同的网络密度

          E(PV)
            ↑
          1 ┤         ● ● ● ● ● ● ● ● ● ● ● ← 低密度，50次迭代收敛
            │    ● ● ●                          ↑
        0.8 ┤  ●                               更快
            │ ●
        0.6 ┤●       ■ ■ ■ ■ ■ ■ ■ ■ ■ ■ ■ ← 中密度，60-70次迭代
            │      ■■
        0.4 ┤    ■
            │  ■
        0.2 ┤ ■      ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ← 高密度，70-80次迭代
            │     ▲▲▲
          0 ┤──▲▲─────────────────────────→ 迭代次数
            0    20   40   60   80   100
```

**结果**：
- 所有密度下，大约 **50 次迭代后都能收敛**
- **低密度收敛最快，高密度最慢**

**为什么？** ——用"消息传播"来理解：

```
低密度网络（稀疏）：              高密度网络（密集）：
  ●   ●   ●   ●                    ●─●─●─●─●
  │   │   │   │                    │╲│╱│╲│╱│
  ●   ●   ●   ●                    ●─●─●─●─●
  │   │   │   │                    │╱│╲│╱│╲│
  ●   ●   ●   ●                    ●─●─●─●─●

固定节点=1 : 信息要传播到全网络    固定节点=1 : 信息要传播到全网络
每迭代一"步"只能传到邻居            每迭代一"步"只能传到邻居
→ 稀疏：路径长（步数多）            → 密集：路径多（每步覆盖面大）
→ 但每步计算量少                    → 但每步计算量多

实际上：
  稀疏网络"收敛快"是因为节点少      密集网络"收敛慢"是因为每个节点有更多
  每个节点只需处理少量邻居           邻居需要参与平均，单轮计算更重
  → 收敛的"轮数"少                  → 收敛的"轮数"更多
```

> **结论**：无论密度高低，50-80 次迭代即可收敛。收敛速度与网络密度成正比（密度越高越慢）。

---

### Fig.6(b)：初始值 vs 收敛速度

**实验问题**：固定节点的初值设成 1 还是 100，对收敛有影响吗？

```
X轴 = 迭代次数
Y轴 = 归一化平均势值
不同曲线 = 不同的固定节点初始值

          E(PV)
            ↑
          1 ┤   ●1 ●10 ●100 ← 三条曲线几乎完全重合！
            │  ●
        0.8 ┤ ●
            │●
        0.6 ┤
            │
        0.4 ┤
            │
        0.2 ┤
            │
          0 ┤────────────────────→ 迭代次数
            0    20   40   60   80
```

**结果**：初值设为 1、10、100，三条收敛曲线**几乎完全重合**！

**为什么？** ——这是调和函数的一个优良性质：

```
调和函数的收敛过程满足"线性性"：

如果所有固定节点初值同时乘以 k：
  → 全场的势值在收敛过程中也近似同时乘以 k
  → 归一化后的 E(PV) 几乎不变
  → 收敛的"轮数"不受影响

类比：
  用50°C的热水和用100°C的热水去加热一碗冷水杯
  → 温度最终的稳定状态不同（一个是温暖，一个是烫）
  → 但达到稳定的时间几乎相同！

  调和函数类似：初值影响最终场的"高度"
  但不影响场的"形状"和收敛速度
```

> **结论**：收敛速度与固定节点初值无关。这意味着我们不需要精确校准固定节点的势值，只要它们相对其他节点有明显差异（满足阈值条件）即可——工程上非常友好。

---

### Fig.6(c)：通信半径 vs 收敛速度

**实验问题**：UAV 通信范围越大还是越小，收敛越快？

```
X轴 = 迭代次数
Y轴 = 归一化平均势值
不同曲线 = 不同的通信半径

          E(PV)
            ↑
          1 ┤    ● ● ● ● ● ● ● ● ● ● ← 小半径，收敛最快
            │   ●
        0.8 ┤  ●
            │ ●
        0.6 ┤●
            │
        0.4 ┤      ■ ■ ■ ■ ■ ■ ■ ■ ■ ■ ← 中半径
            │    ■■
        0.2 ┤  ■
            │ ■
          0 ┤■       ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ← 大半径，收敛最慢
            └──▲▲▲──────────────────→ 迭代次数
            0    20   40   60   80   100
```

**结果**：**通信半径越大，收敛越慢。**

**为什么？**

```
小通信半径(R=50m)：                  大通信半径(R=150m)：
      ●                                ┌──────────●──────────┐
     ╱ ╲                               │         ╱│╲         │
    ●   ●                              │        ╱ │ ╲        │
   ╱ ╲ ╱ ╲                             │       ●  │  ●       │
  ●───●───●                            │      ╱╲  │  ╱╲      │
 ╱ ╲ ╱ ╲ ╱ ╲                           │     ●  ●─●─●  ●     │
●───●───●───●                          │    ╱ ╲╱ ╲╱ ╲╱ ╲    │
                                       │   ●───●───●───●───●   │
每个节点邻居少（3-5个）                └─────────────────────────┘
每次迭代取平均简单                     每个节点邻居多（10-20个）
每次计算量少                           每次迭代取平均复杂
                                       → 每次计算量大
但更关键的是：
  通信半径大 → 信息用更少"步"就能传遍全网
  但因为每步的计算量更大（邻居更多），
  实际总计算量更大

类似于：
  步行穿过整个城市（小步快走）
  vs 
  开车穿过整个城市（大步但每步决策复杂）
  → 步行可能总耗时更少
```

> **结论**：通信半径增大 → 邻居增多 → 单次迭代计算量增大 → 收敛所需总计算量增加。

---

### Fig.6 三组实验的汇总

| 实验 | 变化因素 | 对收敛速度的影响 | 工程启示 |
|------|---------|----------------|---------|
| (a) | 网络密度 ↑ | 收敛变慢 | 密集网络需要更多迭代或更强算力 |
| (b) | 初值大小 ↑ | **不影响** | 初值设置门槛低，工程友好 |
| (c) | 通信半径 ↑ | 收敛变慢 | 半径要权衡：覆盖 vs 收敛速度 |

---

### 📌 论文对 Fig.6 三段验证的总结（原文第646-659行）

论文从这三组实验提炼了三个**关键认识**：

> **1. 迭代次数可收敛且可管理** → 收敛约需 50 轮，可以实现实时势场构建。这对需要高实时性的路由至关重要。

> **2. 初值不影响收敛** → 势场构建不依赖固定节点的值与其他节点有"足够大的差异"。即使固定节点初值只是略高于其他节点，场也能正常构建。

> **3. 密度和邻居数影响性能** → 更低的网络密度和更少的邻居节点 → 更快的收敛。在设计和部署时需要考虑这个 trade-off。

---

## 🧩 第七、八页的完整知识地图

```
第七、八页内容总览
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

III-B 势场构建三步法
├── Step 1: 辅助节点 ──→ 解决速度场的方向性问题
│   └── 每个实物节点配4个虚拟辅助节点，保留方向信息
├── Step 2: 固定节点 ──→ 势场的"锚点"
│   └── 3类：边界 + 轻微影响 + 严重影响
└── Step 3: 迭代计算 ──→ 执行公式(11)，~50轮收敛

III-C 两种路由算法
├── Scheme α：橡皮筋上的小球（时效性优先）
│   ├── 核心公式 (16)：sigmoid-like 方位变换
│   ├── Rank = f(Δθ) × PV → 综合方向+质量
│   └── 机制：方向奖励，隐式绕行
└── Scheme β：探照灯越野车（无损性优先）
    ├── 核心概念：阻挡区域 b
    ├── 机制：先检测阻挡，再选方向，显式绕行
    └── 两个独立 WHILE 循环：感知与决策分离

III-D 复杂度分析
├── 势场构建：O(n·N·D) → 百万级操作，秒级
├── 路由执行：O(H·D) → 百级操作，微秒级
└── 开销：与 GPSR 相当，主要来自 0.5s 间隔的 HELLO 消息

IV 仿真预备
├── 基准选择：GPSR（位置路由）+ AC（蚁群优化）
├── Fig.6(a)：密度↑ → 收敛变慢
├── Fig.6(b)：初值变化 → 不影响收敛
└── Fig.6(c)：半径↑ → 收敛变慢
```

---

## 🏠 课后思考题

1. **辅助节点数量**：为什么选 4 个象限而不是 6 个或 8 个？如果 UAV 的移动模式主要是南北向（如巡逻航线），4 象限还够用吗？你觉得应该怎么调整？

2. **固定节点的阈值**：类型 2 和类型 3 的判据是"ETX 比值超过预设阈值"。这个阈值设多大合适？太大 → 固定节点太少，场缺乏形状。太小 → 太多节点被固定，场变成许多钉子的简单插值。你能想到一个**自适应确定阈值**的方法吗？

3. **Scheme α 的漏洞**：如果一个邻居正对目标（Δθ=0），且 PV 很高（严重干扰），而另一个邻居偏离目标 80°，PV 很低。在什么参数设置下 α 会错误地选择前者？λ₁, λ₂, λ₃ 应该怎么调？

4. **Scheme β 的极端情况**：如果目标方向被阻挡，且阻挡区域覆盖了 350°（几乎全方向被挡），Scheme β 会怎么做？它还能找到路径吗？这暴露了 β 的什么潜在缺陷？

5. **预处理 vs 在线**：势场构建的复杂度 O(n·N·D) 远高于路由的 O(H·D)。如果干扰源快速移动（例如车载干扰机），势场更新频率跟不上干扰变化，路由性能会怎样恶化？你有什么改进思路？

---

> **下一站预告**：第九、十页 — Fig.7-12 的定性验证，包括干扰场的 3D 可视化、两种方案的路径对比、速度场的辅助节点效果展示。那是将"纸上的公式"变成"眼前的画面"的关键一步。
